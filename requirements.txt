# requirements.txt — обновлённый с CTranslate2
# Основные (CTranslate2 режим — без утечки памяти)
ctranslate2>=4.0.0
sentencepiece
sacremoses
sentence-splitter

# API
fastapi
uvicorn[standard]
gradio

# Fallback (PyTorch — только если CT2 модель не сконвертирована)
# torch
# transformers

# Опциональные
huggingface_hub
